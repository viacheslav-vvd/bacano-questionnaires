<div class="mb-6">
  <label for="company_name" class="string optional block mb-1 font-semibold w-full text-sm">
    <%= question.value %>
  </label>

  <%= render "steps/answers_by_type/#{question.answer_type.downcase}", 
             question: question %>
</div>
